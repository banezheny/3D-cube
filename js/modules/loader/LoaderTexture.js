define([

    'three',
    'underscore'

], function (THREE, _) {
    'user strict';

    var LoaderTexture = function (urlMap) {

        var textures = [];

        if (urlMap) {

        }
    }


    return LoaderTexture;
});